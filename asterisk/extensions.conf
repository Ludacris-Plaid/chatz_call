[public]
; === INBOUND ROUTING ===
exten => s,1,NoOp(Connected leg)
same => n,Echo()
same => n,Hangup()
; All numbers normalized to E.164 +1XXXXXXXXXX
exten => _+1NXXNXXXXXX,1,Dial(PJSIP/sipup-trunk/sip:${EXTEN}@sip.sipup.org)
same => n,Hangup()
exten => _1NXXNXXXXXX,1,Dial(PJSIP/sipup-trunk/sip:+1${EXTEN:1}@sip.sipup.org)
same => n,Hangup()
exten => _NXXNXXXXXX,1,Dial(PJSIP/sipup-trunk/sip:+1${EXTEN}@sip.sipup.org)
same => n,Hangup()
exten => _NXXXXXX,1,Dial(PJSIP/sipup-trunk/sip:+1780${EXTEN}@sip.sipup.org)
same => n,Hangup()
exten => 7001,1,Dial(PJSIP/7001)
same => n,Hangup()
exten => 9196,1,Answer()
same => n,Echo()
same => n,Hangup()
